import React, { Component } from 'react';

export default class PhotoUploader extends Component {
    render () {
        return (
            <div>This is the photo uploader.</div>
        );
    }
}